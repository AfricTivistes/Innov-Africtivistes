---
export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={`tabs-list ${className || ''}`}>
  <slot />
</div>

<style>
  .tabs-list {
    display: flex;
    gap: 0.25rem;
  }
</style>
